box: wercker/ruby
services:
  # Include Redis for test suite
  - wercker/redis
build:
  steps:
    # Install Ruby Gems
    - bundle-install

    # Run test suite
    - script:
        name: rspec
        code: bundle exec rspec
