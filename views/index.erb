<% @emoticons.each do |emoticon| %>
  <figure data-clipboard-text="(<%= emoticon['shortcut'] %>)" data-shortcut="<%= emoticon['shortcut'] %>" class="emoticon">
    <img class="icon" src="<%= emoticon['url'] %>" alt="emoticon" />
  </figure>
<% end %>
